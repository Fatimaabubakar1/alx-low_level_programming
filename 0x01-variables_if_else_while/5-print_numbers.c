#include <stdio.h>


/**
 * main - program that prints the alphabet in lowercase and upper case
 *
 * Return: Always 0.
 */

int main(void)
{
	int num;

	for (num = 0; num <=  9; num++)
	{
		printf("%d", num)
	}

	putchar('\n');

	return (0);
}
